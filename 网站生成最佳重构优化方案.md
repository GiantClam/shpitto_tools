# 网站生成最佳重构优化方案

> **基于**：`codex/shpitto-skill-agent-plan` 分支代码走读 + PPT 生成流程参考 + 社区/官方最佳实践
> **前置文档**：`网站生成方案对比评估与最佳方案.md`
> **更新时间**：2026-03-29

---

## 一、Skill 分支代码走读总结

### 1.1 分支新增文件与核心实现

| 文件 | 核心能力 | 行数 |
|------|----------|------|
| `contracts.ts` | `normalizePagesBySiteContract` + `validateGeneratedSiteContract`，硬约束：必须首页、企业必备页、contact lead capture、渐变文本禁令、语言一致性、占位文案检测 | ~400 |
| `route-contract.ts` | canonical 路由映射（/home->/、/product->/products 等），路由去重与规范化 | ~150 |
| `skill-orchestrator.ts` | `orchestrateTemplateAndSectionCandidates`：page-type hardness 注入、section 重排、去重预算、策略建议 | ~350 |
| `page-rule-matrix.ts` | 每页面类型的 nav/hero/content 节奏约束、section 重复预算、关键页面对防同构 | ~250 |
| `serper.ts` | `buildSerperFactPack`：按 required fields 发 query，结果作为 External Fact Pack 注入 prompt | ~200 |
| `structured-input.ts` | `StructuredSiteInput` 类型 + `parseStructuredSiteInput` + `buildStructuredInputPromptPatch` | ~300 |

### 1.2 集成方式（p2w-graph.ts 走读）

当前 Skill 分支的集成方式仍为**单体 builder 内串行调用**：

```
generateP2WProject()
  |-- parseStructuredSiteInput(prompt)           // 全局一次
  |-- buildSerperFactPack(prompt, structured)     // 全局一次
  |-- architectNode()
  |     |-- normalizePagesBySiteContract()        // pre-architect 契约规范化
  |     +-- LLM blueprint 生成
  +-- builderNode()
        |-- normalizePagesBySiteContract()        // pre-template 契约规范化
        |-- orchestrateTemplateAndSectionCandidates()
        |-- resolveTemplatePlan()
        |-- normalizePagesBySiteContract()        // post-template 契约规范化
        |-- for each page:                        // 串行遍历页面
        |     +-- for each section (concurrency=3):
        |           +-- generateSection()
        |-- unifyNavbar/Footer/Theme
        |-- validateGeneratedSiteContract()       // Contract Gate
        |-- evaluateGenerationQa()                // QA Gate
        +-- auditSitePayload()                    // Payload Audit
```

### 1.3 关键发现

1. **Serper 是全局调用**：在 `generateP2WProject` 入口处一次性调用，所有页面共享同一份 fact pack
2. **structuredInput 是全局传递**：通过 graph state annotation 传递给所有节点，未按页面拆分
3. **section 并发已存在**：`defaultSectionConcurrency = 3`，但页面间仍串行
4. **契约规范化执行 3 次**：pre-architect、pre-template、post-template
5. **skill-orchestrator 是纯函数**：接收 blueprint + page rules，输出编排建议，不持有状态

---

## 二、PPT 生成流程参考与迁移分析

### 2.1 PPT 生成架构（从三个 Skill 提取）

基于 MiniMax pptx-generator、MiniMax pptx-plugin、Anthropic pptx 三个 skill 的分析：

```
[Orchestra Skill]  -- 总编排
    |-- 分类每页 slide type（cover/toc/content/section-divider/summary）
    |-- 强制视觉多样性（同类型不超过 45%）
    |-- 分配 template_family（防重复）
    +-- 输出：per-slide 生成计划
         |
         v
[Parallel Slide Generators]  -- 每页一个 skill/agent
    |-- Slide Agent: cover
    |-- Slide Agent: content-grid
    |-- Slide Agent: timeline
    |-- Slide Agent: quote-stat
    +-- ...
         |
         v
[Design Skills]  -- 横切关注点
    |-- color-font-skill（配色 + 字体）
    |-- design-style-skill（圆角/间距/风格配方）
    +-- slide-making-skill（PptxGenJS API 规范）
         |
         v
[QA]
    |-- Per-slide: 文本溢出、元素重叠、占位符残留
    +-- Per-deck: 类型分布、模板重复率、色板一致性
```

### 2.2 PPT 到网站的模式映射

| PPT 概念 | 网站对应 | 映射关系 |
|----------|----------|----------|
| Deck（整个演示文稿） | Site（整个网站） | 1:1 |
| Slide（单页幻灯片） | Page（单个页面） | 1:1 |
| Slide Type（cover/content/summary） | Page Type（home/products/about/contact/privacy） | 1:1 |
| Section（幻灯片内区块） | Section（页面内区块：hero/features/cta/footer） | 1:1 |
| Template Family | Style Profile / Block Template | 1:1 |
| Orchestra Skill | Site Planner | 1:1 |
| Slide Agent | Page Builder Agent | 1:1 |
| Design Skills（配色/字体/风格） | Theme Skills（主题/导航/页脚） | 横切关注点 |
| Per-slide QA | Per-page Contract + QA | 1:1 |
| Per-deck QA | Site-level QA Gate + Payload Audit | 1:1 |

### 2.3 PPT 方案的核心启示

1. **每页一个 Skill/Agent**：PPT 为每种 slide type 建独立 agent，网站应为每种 page type 建独立 Page Builder
2. **Orchestra 先行**：先完成全局编排（类型分配、多样性约束、模板分配），再并行生成
3. **横切关注点独立**：配色/字体/风格作为独立 skill 被所有 agent 引用，而非嵌入每个 agent
4. **两级 QA**：生成中做 per-page 校验（快速、同步），组装后做 per-site 校验（全面、可异步）

### 2.4 PPT 视觉质量方案的额外参考

基于 `2026-03-28-ppt-visual-improvement-strategy.md` 的关键经验：

| PPT 痛点 | 网站同类问题 | PPT 治理方案 | 网站可借鉴 |
|----------|-------------|-------------|-----------|
| slide_type 分布单调（grid_3 占 37.5%） | 页面 section 结构同构 | 子类型拆分 + 分布门禁（单类型不超 45%） | page-rule-matrix hardness + 关键页面对防同构 |
| 所有页背景色完全相同 | 所有页面视觉风格雷同 | 分场景 Profile + 色板自动匹配 | style profile 多样化 + 页面级主题微调 |
| 模板残留/占位图 | 模板占位文案泄漏 | 占位符检测 + 二次替换 | contract 占位文案检测（已有） |
| 单一渲染路径 | 单体 builder 串行 | 三级管线（快速/标准/精细） | 分层策略（template_first/hybrid/llm_first） |
| 视觉 QA 缺失 | 语义 QA 不够细 | 多维评分替代二值门禁 | 三层门禁 + 加权评分（已有） |

---

## 三、社区与官方最佳实践

### 3.1 LangGraph Send API / Map-Reduce（官方推荐）

LangGraph 的 `Send` API 是实现动态并行的核心机制：

```typescript
import { Send } from "@langchain/langgraph";

// Planner 节点：动态 fan-out 到并行 Page Builder
function plannerNode(state) {
  return state.pages.map(page =>
    new Send("pageBuilder", {
      page,
      theme: state.theme,
      factPack: state.factPack
    })
  );
}
```

关键特性：
- **动态并行**：运行时决定并行数量（页面数）
- **独立 state**：每个 Send 目标拥有独立 state，互不干扰
- **自动汇聚**：通过 `Annotation` 的 reducer 自动合并结果

### 3.2 Subgraph State 隔离

```typescript
// Page Builder 作为独立 subgraph
const pageBuilderGraph = new StateGraph(PageBuilderState)
  .addNode("scopedRag", scopedRagNode)
  .addNode("generateSections", generateSectionsNode)
  .addNode("pageValidation", pageValidationNode)
  .addEdge(START, "scopedRag")
  .addEdge("scopedRag", "generateSections")
  .addEdge("generateSections", "pageValidation")
  .addEdge("pageValidation", END);
```

### 3.3 HITL Interrupt

```typescript
import { interrupt } from "@langchain/langgraph";

function plannerNode(state) {
  const plan = generateSitePlan(state);
  // 暂停执行，等待用户确认
  const approved = interrupt({ plan, message: "请确认站点结构" });
  if (!approved) throw new Error("用户拒绝");
  return plan;
}
```

### 3.4 错误处理与部分成功

```typescript
// RetryPolicy per node
const graph = new StateGraph(State)
  .addNode("pageBuilder", pageBuilderNode, {
    retryPolicy: { maxAttempts: 3, backoffFactor: 2 }
  });

// 单页失败不影响其他页面
function pageBuilderNode(state) {
  try {
    return generatePage(state.page);
  } catch (e) {
    return {
      page: buildFallbackPage(state.page),
      errors: [e.message]
    };
  }
}
```

---

## 四、最佳重构方案

### 4.1 目标架构

```
POST /api/creation
    |
    v
[Input Parser]
    |-- parseStructuredSiteInput()
    +-- 输出：StructuredSiteInput + raw prompt
    |
    v
[Site Planner]  -- 替代当前 architect，融合 skill-orchestrator
    |-- Route Contract 规范化
    |-- Site Contract 校验（required pages 补齐）
    |-- Page Rule Matrix 注入（hardness + 防同构）
    |-- orchestrateTemplateAndSectionCandidates()
    |-- resolveTemplatePlan()
    |-- 为每页生成 Scoped RAG Query
    +-- 输出：SitePlan
         |  {
         |    theme, globalChrome (navbar/footer),
         |    pages: [{
         |      path, pageType, sections, templatePlan,
         |      hardnessRules, ragQueries, strategy
         |    }]
         |  }
         |
         v
[HITL Breakpoint]  -- 可选：用户确认站点结构
         |
         v
[Send API Fan-Out]  -- LangGraph Send，每页一个并行任务
    |
    |-- [Page Builder: home]        -- 独立 subgraph
    |     |-- Scoped RAG（仅检索 home 相关信息）
    |     |-- Section 生成（hero/features/testimonials/cta）
    |     +-- Page Contract 校验
    |
    |-- [Page Builder: products]    -- 独立 subgraph
    |     |-- Scoped RAG（检索产品规格/参数）
    |     |-- Section 生成（catalog/specs/comparison）
    |     +-- Page Contract 校验
    |
    |-- [Page Builder: about]       -- 独立 subgraph
    |-- [Page Builder: contact]     -- 独立 subgraph
    |-- [Page Builder: privacy]     -- 独立 subgraph
    +-- ...
    |
    v  (Annotation reducer 自动汇聚)
[Global Assembler]  -- Reduce 阶段
    |-- 统一 Navbar / Footer（来自 Site Planner 的 globalChrome）
    |-- Link Graph 规范化（href canonical 化 + 内链校验）
    |-- Theme 锁定（确保所有页面主题一致）
    |-- Site Contract Gate（全站级契约校验）
    |-- QA Gate（coverage/link/theme/semantic/locale 评分）
    +-- Payload Audit（unknown block/fallback block/结构相似度）
    |
    v
[Persist + Sandbox Preview]
```

### 4.2 核心设计：每页一个 Page Builder Subgraph

借鉴 PPT "每页一个 Skill" 的思路，每种 page type 拥有专属 Page Builder：

```typescript
// Page Builder Subgraph 定义
const PageBuilderState = Annotation.Root({
  // 输入（从 Site Planner 通过 Send 传入）
  page: Annotation<PagePlan>,
  theme: Annotation<ThemeConfig>,
  globalChrome: Annotation<GlobalChrome>,
  // 中间状态
  ragContext: Annotation<string>,
  sections: Annotation<GeneratedSection[]>,
  // 输出
  result: Annotation<PageResult>,
  errors: Annotation<string[]>,
});

const pageBuilderGraph = new StateGraph(PageBuilderState)
  .addNode("scopedRag", scopedRagNode)
  .addNode("generateSections", generateSectionsNode)
  .addNode("pageContract", pageContractNode)
  .addEdge(START, "scopedRag")
  .addEdge("scopedRag", "generateSections")
  .addEdge("generateSections", "pageContract")
  .addEdge("pageContract", END)
  .compile();
```

**Page Type 专属逻辑**通过 `page.pageType` 分发，以下为每种页面类型的完整规格：

---

#### Home 页（首页）

| 维度 | 规格 |
|------|------|
| **Section 顺序** | navigation -> hero -> story -> approach -> products -> socialproof -> cta -> footer |
| **必须 Section** | navigation, hero, cta, footer |
| **推荐 Section** | story（品牌叙事）, products（核心产品展示）, socialproof（客户信任） |
| **Hero 约束** | 预设 H01（split showcase），双栏布局，右侧媒体，spacious 密度 |
| **内容结构** | triple 三栏，cards 卡片列表 |
| **Section 去重预算** | navigation:1, hero:1, products:1, approach:1, socialproof:1, footer:1 |
| **Composition Presets** | H01(hero) -> F01/F02(features) -> P01(products) -> L01/T01(trust) -> C02(CTA) -> FT01(footer) |
| **RAG 字段** | company, core_value, main_products（前 3 个产品概要） |
| **契约规则** | 首页必须存在（site contract error）、hero 必须包含公司名称 |

---

#### Products 页（产品页）

| 维度 | 规格 |
|------|------|
| **Section 顺序** | navigation -> hero -> products -> approach -> socialproof -> contact -> cta -> footer |
| **必须 Section** | navigation, hero, products, footer |
| **推荐 Section** | approach（技术优势）, socialproof（客户评价）, contact（询价入口） |
| **Hero 约束** | 预设 H02（image-led），双栏布局，左侧媒体，normal 密度 |
| **内容结构** | dual 双栏，rows 行列表 |
| **Section 去重预算** | navigation:1, hero:1, products:1, approach:1, socialproof:1, contact:1, footer:1 |
| **Composition Presets** | H02(hero) -> P01/P02(catalog/filter-grid) -> S01(specs) -> CP01(comparison) -> CT01(contact) |
| **RAG 字段** | product_list, specs, pricing, comparison_data |
| **契约规则** | 当 structuredInput.products 存在时，products section 必须展示所有产品 |
| **防同构** | 与 /core-product 页为关键页面对，检测结构相似度 |

---

#### Solutions 页（解决方案页）

| 维度 | 规格 |
|------|------|
| **Section 顺序** | navigation -> hero -> approach -> products -> story -> socialproof -> contact -> cta -> footer |
| **必须 Section** | navigation, hero, approach, footer |
| **推荐 Section** | products（相关产品）, story（场景描述）, contact |
| **Hero 约束** | 预设 H03（centered），split 布局，右侧媒体，normal 密度 |
| **内容结构** | split 分栏，rows 行列表 |
| **Section 去重预算** | navigation:1, hero:1, approach:1, products:1, story:1, contact:1, footer:1 |
| **Composition Presets** | H03(hero) -> F01(features/benefits) -> CS01(case-split) -> CN01(content) -> C01(CTA-form) |
| **RAG 字段** | use_cases, benefits, industry_applications |
| **契约规则** | approach section 必须包含至少 3 个方案要点 |
| **防同构** | 与 /cases 页为关键页面对，structure 和 list 类型必须不同 |

---

#### Cases 页（案例页）

| 维度 | 规格 |
|------|------|
| **Section 顺序** | navigation -> hero -> socialproof -> products -> story -> contact -> cta -> footer |
| **必须 Section** | navigation, hero, socialproof, footer |
| **推荐 Section** | products（相关产品引用）, story（详细案例叙事）, contact |
| **Hero 约束** | 预设 H02，双栏布局，左侧媒体，tiles 磁贴列表 |
| **内容结构** | dual 双栏，tiles 磁贴 |
| **Composition Presets** | H02(hero) -> T02(testimonial-grid) -> CS01(case-split) -> ST01(stats) -> C01(CTA) |
| **RAG 字段** | customer_stories, results_data, industry |
| **契约规则** | 当 structuredInput.cases 存在时必须展示 |
| **防同构** | 与 /solutions 页为关键页面对 |

---

#### About 页（关于页）

| 维度 | 规格 |
|------|------|
| **Section 顺序** | navigation -> hero -> story -> approach -> socialproof -> contact -> cta -> footer |
| **必须 Section** | navigation, hero, story, footer |
| **推荐 Section** | approach（核心能力）, socialproof（资质认证）, TM01（团队展示） |
| **Hero 约束** | 预设 H03（centered），单栏，背景图，spacious 密度 |
| **内容结构** | dual 双栏，cards 卡片 |
| **Composition Presets** | H03(hero) -> CN01/CN02(content-story) -> TL01(timeline) -> TM01(team) -> L01(trust-logos) |
| **RAG 字段** | history, team, mission, certifications |
| **契约规则** | 无硬约束，但 story section 推荐包含公司历史时间线 |

---

#### Contact 页（联系页）

| 维度 | 规格 |
|------|------|
| **Section 顺序** | navigation -> hero -> contact -> socialproof -> story -> cta -> footer |
| **必须 Section** | navigation, hero, contact（含 lead capture）, footer |
| **推荐 Section** | socialproof（信任背书）, story（服务承诺） |
| **Hero 约束** | 预设 H03，单栏，compact 密度，背景图 |
| **内容结构** | single 单栏，rows 行列表 |
| **Section 去重预算** | navigation:1, hero:1, contact:1, cta:1, footer:1（最精简页面） |
| **Composition Presets** | H03(hero) -> CT01/FRM01(contact-form) -> MP01(map) -> C02(CTA-banner) |
| **RAG 字段** | address, phone, email（通常从 structured.company 直接获取） |
| **契约规则** | **contact section 必须有 lead capture 表单**（site contract error）、**禁止渐变文本**（text-gradient/bg-clip-text） |
| **特殊处理** | 强制 template_first（不经 LLM 生成），hardness 注入 `forbidGradientText: true` |

---

#### Privacy/Terms 页（法律页）

| 维度 | 规格 |
|------|------|
| **Section 顺序** | navigation -> story -> footer（最简结构） |
| **必须 Section** | navigation, story（承载政策文本）, footer |
| **Hero 约束** | 预设 H03，单栏，compact 密度，无媒体 |
| **内容结构** | single 单栏，rows 行 |
| **Section 去重预算** | navigation:1, story:1, footer:1 |
| **Composition Presets** | CN01(content-centered) |
| **RAG 字段** | 无（模板化生成） |
| **契约规则** | 企业站必须存在 /privacy 页（site contract error） |
| **特殊处理** | 纯文本页，优先 template_first + deterministic fallback |

---

#### Pricing 页（定价页）

| 维度 | 规格 |
|------|------|
| **Section 顺序** | navigation -> hero -> products -> socialproof -> contact -> cta -> footer |
| **必须 Section** | navigation, hero, products（定价表）, footer |
| **Hero 约束** | 预设 H02，双栏，右侧媒体，cards 列表 |
| **内容结构** | triple 三栏，cards 卡片 |
| **Composition Presets** | H02(hero) -> PRC01(pricing-3-tier) -> F01(features) -> T01(testimonials) -> C01(CTA) |
| **RAG 字段** | pricing_tiers, feature_comparison |

---

#### Support/FAQ 页（支持页）

| 维度 | 规格 |
|------|------|
| **Section 顺序** | navigation -> hero -> story -> approach -> contact -> cta -> footer |
| **必须 Section** | navigation, hero, footer |
| **推荐 Section** | story（知识库入口）, approach（常见问题）, contact（工单入口） |
| **Hero 约束** | 预设 H03，单栏，背景图 |
| **Composition Presets** | H03(hero) -> Q01/Q02(FAQ-accordion) -> CT01(contact) -> C02(CTA) |
| **RAG 字段** | faq_list（优先从 structuredInput.faqs 获取） |

### 4.3 RAG 的作用与知识接入设计

#### 4.3.1 RAG 解决什么问题

网站生成的核心难点是**信息密度不足**：用户一句 "为 XX 公司生成官网"，LLM 无法凭空编造准确的产品参数、客户案例、公司历史。RAG（检索增强生成）的作用是**在生成前为 LLM 补充事实性信息**，使输出内容具体、准确、有说服力，而非充斥 "Your Brand Here" 等占位文案。

#### 4.3.2 三层信息来源（优先级递减）

| 层级 | 信息来源 | 何时可用 | 作用 |
|------|----------|---------|------|
| **L1：StructuredSiteInput** | 用户直接提交的结构化数据（公司名、产品列表、案例、FAQ） | 用户主动提供时 | 最高质量，无需检索 |
| **L2：企业知识库** | 企业自有的知识库/CMS/PIM（通过 API 接入） | 企业已部署知识库时 | 高质量，需要集成 |
| **L3：公网搜索** | Serper/Google 搜索公开信息 | 默认可用 | 兜底，质量不可控 |

**关键原则：L1 > L2 > L3，高优先级信息已覆盖的字段不再向低优先级检索。**

#### 4.3.3 Scoped RAG 替代全局 Fact Pack

当前 Skill 分支的问题：Serper 在入口全局调用一次，所有结果注入主 prompt，导致 token 膨胀和 "lost in the middle" 退化。

重构为 per-page scoped RAG：

```typescript
// Page Builder 内的 RAG 节点
async function scopedRagNode(state: PageBuilderState) {
  const { page, structured, knowledgeBaseClient } = state;

  // L1：StructuredSiteInput 已提供的字段直接使用，不检索
  const coveredFields = detectCoveredFields(structured, page.pageType);

  // L2：企业知识库（如果已接入）
  let kbContext = '';
  if (knowledgeBaseClient) {
    const kbQueries = generateKBQueries(page, coveredFields);
    kbContext = await knowledgeBaseClient.retrieve(kbQueries);
    coveredFields.push(...extractCoveredFields(kbContext));
  }

  // L3：公网搜索（仅补充 L1+L2 未覆盖的字段）
  let searchContext = '';
  const missingFields = getMissingFields(page.pageType, coveredFields);
  if (missingFields.length > 0) {
    const queries = generateSearchQueries(page, missingFields);
    searchContext = await buildSerperFactPack(queries, { budget: 2 });
  }

  return { ragContext: [kbContext, searchContext].filter(Boolean).join('\n') };
}
```

#### 4.3.4 Per-page RAG 字段矩阵

| Page Type | 必需信息字段 | L1 来源 | L2/L3 检索 query 示例 |
|-----------|-------------|---------|----------------------|
| home | company, core_value, main_products | structured.company, structured.products[0..2] | "{company} 简介 核心业务" |
| products | product_list, specs, pricing | structured.products | "{product} 技术规格 参数" |
| solutions | use_cases, benefits | structured.cases | "{company} 行业解决方案" |
| cases | customer_stories, results | structured.cases | "{company} 客户案例 效果" |
| about | history, team, mission | structured.company.summary | "{company} 发展历程 团队" |
| contact | address, phone, email | structured.company.address/email/phone | 无需检索（纯表单页） |
| privacy | -- | -- | 无需检索（模板化） |

#### 4.3.5 企业知识库接入接口设计

预留标准接口，但**不依赖知识库存在**：

```typescript
interface KnowledgeBaseClient {
  retrieve(queries: string[]): Promise<string>;  // 返回格式化文本
  isAvailable(): boolean;
}

// 配置项（均可选）
KNOWLEDGE_BASE_ENABLED=false              // 总开关
KNOWLEDGE_BASE_ENDPOINT=                  // API 地址
KNOWLEDGE_BASE_API_KEY=                   // 认证
KNOWLEDGE_BASE_TYPE=vector|api|cms        // 接入类型
```

无知识库时的降级策略：
- L1 StructuredSiteInput 充足（如 products/cases/faqs 都有）：**不调用任何外部检索**，直接生成
- L1 部分缺失：**仅对缺失字段调用 L3 Serper 搜索**
- L1 完全缺失（仅 prompt）：**全字段 Serper 搜索**（当前行为）

### 4.4 模板体系在新架构中的使用方式

#### 4.4.1 现有模板资产盘点

当前系统已积累大量模板资产，重构必须完整复用：

| 资产类型 | 文件 | 数量 | 用途 |
|---------|------|------|------|
| Style Profiles | `template-factory/library/style-profiles.generated.json` | **35 个** | 整站风格模板（含 theme + 每页 section 模板） |
| Block Catalog | `template-factory/library/template-block-catalog.generated.json` | **1,241 个 block** | 单 section 级模板（含 props + editable fields） |
| Template-Exclusive Components | `template-factory/library/template-exclusive-components.generated.json` | **1,394 个组件** | 从真实网站抽取的专属组件 |
| Generation Contracts | `template-factory/library/template-generation-contracts.generated.json` | **29 份契约** | 每个 profile 的 per-page section 生成契约 |

#### 4.4.2 模板解析五层级联（完整保留）

每个 section 生成时，按以下层级依次尝试匹配模板：

```
Layer 1: full-site  ← Style Profile 的 siteTemplates 完整覆盖所有页面
    |
    v (未匹配)
Layer 2: page       ← Style Profile 的 pageSpecs 匹配当前页面类型
    |
    v (未匹配)
Layer 3: section    ← Style Profile 的 templates[kind] 匹配当前 section kind
    |
    v (未匹配)
Layer 4: block      ← Block Catalog 中按 profileId + kind 查找
    |
    v (未匹配)
Layer 5: llm        ← LLM 工具化生成（最慢、最灵活）
```

#### 4.4.3 Section 生成三策略在 Page Builder 中的保留

每个 Page Builder 内部的 section 生成**完整保留**现有三策略逻辑：

```
generateSectionsNode(state):
  for each section in state.page.sections (并发 concurrency=3):
    |
    |-- shouldTemplateFirstForSection(section)?
    |     |
    |     YES --> createTemplateSectionResult():
    |     |         1. selectStyleProfile(prompt)
    |     |         2. inferSectionKind(section.type)
    |     |         3. resolveTemplateCandidate() -- 五层级联
    |     |         4. personalizeTemplateProps() -- 品牌替换 + 本地化
    |     |         5. resolveEditableFieldContracts() -- 可编辑字段
    |     |         return { block, templateMeta, editableFields }
    |     |
    |     NO --> callLlm(builderTool):
    |              1. 构建 section prompt（含 page context + RAG context）
    |              2. LLM 工具化输出 section block
    |              3. 解析修复（JSON parse + schema validate）
    |              4. 失败 --> retry --> 最终 fallback 到 template
    |
    +-- 所有策略最终 fallback: buildDeterministicFallbackBlock()
          基于 section token 返回硬编码 block（Navbar/HeroSplit/CardsGrid 等）
```

**关键决策规则（shouldTemplateFirstForSection）**：
- contact/cta/footer/lead section：**强制 template_first**（不经 LLM）
- navigation section：**强制 template_first**
- hero section：优先 template（有 compositionPreset 约束）
- 其他 section：按策略决定（template_first/hybrid/llm_first）

#### 4.4.4 模板个性化流程（personalizeTemplateProps）

模板命中后不是直接使用原始 props，而是经过多步个性化：

1. **品牌名替换**：从 prompt 提取品牌名，替换模板中的源品牌 token（如 "Siemens" -> 用户品牌）
2. **section kind 专属改写**：hero 的标题/副标题/CTA、products 的产品名/描述
3. **可编辑字段契约应用**：将 EditableFieldContract 中标记的 text/image/link 字段开放给前端编辑
4. **中文本地化**：检测 prompt 语言，必要时将英文模板内容替换为中文
5. **语义清洗**：去除指令性噪声、修复断句

#### 4.4.5 模板资产在架构各层的使用位置

| 架构层 | 使用的模板资产 | 调用函数 |
|--------|-------------|---------|
| **Site Planner** | Style Profiles（选择整站风格） | `selectStyleProfile(prompt)` |
| **Site Planner** | Generation Contracts（获取 per-page section 契约） | `resolvePublishedPageGenerationContract()` |
| **Site Planner** | Template Resolver（确定解析层级 + page section kinds） | `resolveTemplatePlan()` |
| **Page Builder** | Block Catalog + Exclusive Components（单 section 模板匹配） | `resolveSectionTemplateAsset()` |
| **Page Builder** | 模板个性化（品牌替换 + 本地化） | `personalizeTemplateProps()` |
| **Page Builder** | 确定性 Fallback Block（无模板时兜底） | `buildDeterministicFallbackBlock()` |
| **Global Assembler** | SiteStyleShell（nav/footer block type、motion profile） | 从 template plan 读取 |

### 4.5 两阶段质量门禁

借鉴 PPT 的 per-slide + per-deck 双层 QA：

**阶段一：Per-Page（同步，在 Page Builder 内）**

```typescript
async function pageContractNode(state: PageBuilderState) {
  const issues = [];

  // 1. Schema 校验：section 结构是否符合 page type 规则
  const schemaResult = validatePageSchema(state.page, state.sections);
  issues.push(...schemaResult.errors);

  // 2. 占位文案检测
  const placeholders = detectPlaceholderText(state.sections);
  issues.push(...placeholders);

  // 3. Page-type 专属校验
  if (state.page.pageType === 'contact') {
    if (!hasLeadCapture(state.sections)) {
      issues.push({ level: 'error', msg: 'contact 页缺少 lead capture' });
    }
    if (hasGradientText(state.sections)) {
      issues.push({ level: 'error', msg: 'contact 页禁止渐变文本' });
    }
  }

  // 4. 失败处理：error 级别触发 fallback
  const hasErrors = issues.some(i => i.level === 'error');
  if (hasErrors) {
    return {
      result: buildFallbackPage(state.page),
      errors: issues.map(i => i.msg)
    };
  }

  return { result: assemblePageResult(state), errors: [] };
}
```

**阶段二：Per-Site（在 Global Assembler 内）**

- Site Contract Gate：required pages 完整性、路由唯一性
- QA Gate：coverage/link/theme/semantic/locale 加权评分
- Payload Audit：unknown block、fallback block 残留、结构相似度
- 关键页面对防同构检测

### 4.6 Skills 详细设计（参考 pptx-plugin 模式）

> 以下 skill 设计参考 MiniMax pptx-plugin 的 agent/skill 分解模式：
> - **Orchestra Skill** -> Site Planner Skill
> - **Slide Agent（cover/content/summary/section-divider/toc）** -> Page Builder Agent（home/products/solutions/cases/about/contact/privacy）
> - **Design Skills（color-font/design-style/slide-making）** -> Cross-cutting Skills（theme/template-matching/section-generation/navbar-footer/link）
>
> 每个 skill 包含：核心能力、设计原则、输入输出契约、质量检查清单、禁止事项。

---

#### 4.6.1 Site Planner Skill（总编排，对应 ppt-orchestra-skill）

**核心能力**：你是企业网站架构专家，负责将用户需求转化为完整的站点生成计划。你决定站点包含哪些页面、每页包含哪些 section、使用什么风格模板、以及每页需要检索什么信息。

**设计原则**：

1. **结构多样性优先**：不同页面类型必须有明显不同的 section 组合和布局节奏，避免"所有页面看起来一样"
2. **模板匹配优先**：优先使用已有的 35 个 Style Profile 和 1,241 个 Block 模板，仅在无匹配时才走 LLM 生成
3. **信息完整性**：确保每页的 RAG query 覆盖该页面所需的核心信息字段
4. **企业站合规**：必须包含 home/about/contact/privacy 四个基础页面

**输入**：
```typescript
{
  prompt: string;                    // 用户原始需求
  structuredInput: StructuredSiteInput;  // 结构化数据（可选字段）
  knowledgeBaseAvailable: boolean;   // 是否有企业知识库
}
```

**输出**：
```typescript
interface SitePlan {
  theme: ThemeConfig;                // 锁定的全站主题
  styleProfileId: string;           // 选中的 Style Profile
  resolutionLayer: 'full-site' | 'page' | 'section' | 'block' | 'llm';
  pages: PagePlan[];                // 每页生成计划
  linkGraph: SiteLinkGraph;         // 全站链接图
  globalChrome: {                   // 全局 navbar/footer 配置
    navigationBlockType: string;
    footerBlockType: string;
    motionProfile: 'none' | 'subtle' | 'showcase' | 'immersive';
  };
}

interface PagePlan {
  path: string;                     // canonical 路由（如 /、/products）
  pageType: EnterprisePageType;     // home | products | solutions | ...
  sections: SectionPlan[];          // 有序 section 列表
  strategy: 'template_first' | 'hybrid' | 'llm_first';
  hardnessRules: PageHardnessRules; // 页面级约束
  ragQueries: string[];             // 该页面的 RAG 检索 query
  compositionPresets: string[];     // 如 ['H01', 'F01', 'P01', 'C02', 'FT01']
}
```

**关键决策流程**：
1. `selectStyleProfile(prompt)` — 35 个候选，8 层评分（keyword/reference/identity/taxonomy/domain/viewport/intent/quality）
2. `resolveTemplatePlan()` — 确定解析层级，输出每页 section skeleton
3. `normalizePagesBySiteContract()` — 补齐企业站必备页面
4. `orchestrateTemplateAndSectionCandidates()` — hardness 注入 + section 去重 + 防同构
5. 为每页生成 ragQueries（基于 pageType + structuredInput 缺失字段）

**质量检查清单**：
- [ ] 站点包含 home/about/contact/privacy 四个基础页面
- [ ] 所有路由已 canonical 化（无 /home、/product 等别名）
- [ ] 无重复路由
- [ ] 关键页面对（products vs core-product、solutions vs cases）的 section 组合不同
- [ ] 每页 section 数量在 3-8 之间
- [ ] 总 section 数不超过 48（企业站不超过 36）

**禁止事项**：
- 禁止生成超过 10 个页面
- 禁止同一 section kind 在单页出现超过其 repeatBudget
- 禁止跳过 Route Contract 规范化

#### 4.6.2 Theme Skill（主题管理，对应 color-font-skill + design-style-skill）

**核心能力**：你是网站视觉设计专家，负责为整个站点建立统一的视觉语言。你的输出将被所有 Page Builder 只读引用，确保跨页面的视觉一致性。

**设计原则**：

1. **一致性第一**：所有页面共享同一套配色、字体、间距、圆角体系
2. **来源优先级**：Style Profile 的 SiteStyleShell > architect blueprint theme > 默认值
3. **不可变性**：Theme 一旦锁定，Page Builder 不可修改，仅 Global Assembler 可做最终一致性校验

**输入**：
```typescript
{
  architectTheme: Record<string, unknown>;  // LLM architect 输出的主题建议
  siteStyleShell: SiteStyleShell;           // Style Profile 的视觉壳层
  prompt: string;                           // 用户主题偏好提取
}
```

**输出**：
```typescript
interface ThemeConfig {
  colors: {
    primary: string;        // 主色
    secondary: string;      // 辅色
    accent: string;         // 强调色
    background: string;     // 背景色
    surface: string;        // 卡片/面板色
    text: string;           // 正文色
    textMuted: string;      // 次要文字色
  };
  typography: {
    fontFamily: string;     // 主字体
    headingFontFamily: string;  // 标题字体
    baseFontSize: string;   // 基础字号
    lineHeight: number;     // 行高
  };
  spacing: {
    sectionPadding: string; // section 间距
    containerMaxWidth: string;  // 容器最大宽度
    cardGap: string;        // 卡片间距
  };
  borderRadius: string;     // 全局圆角
  motionProfile: 'none' | 'subtle' | 'showcase' | 'immersive';
}
```

**合并规则**：
1. `siteStyleShell.theme` 中的字段直接覆盖默认值
2. `architectTheme` 中与 shell 不冲突的字段补充合并
3. 用户 prompt 中的显式偏好（如"深色主题"、"科技感"）触发预设覆盖
4. `motionProfile` 从 shell 继承，无 shell 时默认 `subtle`

**质量检查清单**：
- [ ] 主色与背景色对比度 >= 4.5:1（WCAG AA）
- [ ] 正文色与背景色对比度 >= 7:1（WCAG AAA）
- [ ] 字体族已指定 fallback（如 `"Inter, system-ui, sans-serif"`）
- [ ] motionProfile 与站点类型匹配（企业站不应为 immersive）

**禁止事项**：
- 禁止 Page Builder 修改 theme 的任何字段
- 禁止使用未在 theme 中定义的颜色硬编码

#### 4.6.3 Template Matching Skill（模板匹配，对应 slide-making-skill 的模板选择部分）

**核心能力**：你是模板匹配引擎，负责为每个 section 从 1,241 个 block 模板和 35 个 style profile 中找到最佳匹配。你的目标是最大化模板命中率，减少 LLM 调用。

**设计原则**：

1. **五层级联，逐层降级**：full-site -> page -> section -> block -> llm，高层命中即停止
2. **同族优先**：同一 style family 的模板优先于跨族模板
3. **kind 精确匹配**：section kind（navigation/hero/story/...）必须精确匹配，不做跨 kind 模糊匹配
4. **个性化必做**：模板命中后必须经过品牌替换和本地化，不可直接使用原始 props

**输入**：
```typescript
{
  prompt: string;
  profileId: string;           // 选中的 Style Profile ID
  pagePath: string;            // 当前页面路由
  pageType: EnterprisePageType;
  sectionType: string;         // block type（如 "HeroSplit"、"CardsGrid"）
  sectionId: string;           // section 唯一标识
  sectionKind: SectionKind;    // 推断的 section kind
}
```

**输出**：
```typescript
interface ResolvedSectionTemplateAsset {
  block: {
    type: string;              // block type
    props: Record<string, unknown>;  // 已个性化的 props
  };
  layer: 'full-site' | 'page' | 'section' | 'block' | 'style-family';
  editableFields: EditableFieldContract;
  templateMeta: {
    profileId: string;
    sourceBlockType: string;
    personalizationApplied: boolean;
  };
} | null  // null 表示无模板命中，需走 LLM
```

**五层级联详细逻辑**：

```
Layer 1: Page-level
  从 profile.pageSpecs 中查找 pageType 匹配的页面
  在该页面的 ordinal sections 中查找 sectionId 匹配的 block
  命中条件：profileId + pageType + sectionId 三重匹配

Layer 2: Section-level
  从 profile.templates[kind] 中查找 sectionKind 匹配的模板
  命中条件：profileId + sectionKind 双重匹配

Layer 3: Block-level
  从 Block Catalog（1,241 entries）中查找：
    profileId 匹配 + sectionKind 匹配
  命中条件：catalogEntry.profileId + catalogEntry.kind

Layer 4: Style-family sibling
  从同一 styleFamily 的其他 profile 中重复 Layer 1-3
  命中条件：同族 profile 的任意层级命中

Layer 5: LLM fallback
  无模板命中，返回 null，由 Section Generation Skill 走 LLM 路径
```

**个性化流程（命中后必做）**：
1. 深拷贝模板 props（避免污染原始模板）
2. 品牌名替换：从 prompt 提取品牌名，替换模板中的源品牌 token
3. Section kind 专属改写：hero 的标题/副标题/CTA、products 的产品名/描述
4. 可编辑字段契约应用：标记 text/image/link/style 可编辑 slot
5. 中文本地化：检测 prompt 语言，英文模板内容替换为中文
6. 语义清洗：去除指令性噪声、修复断句

**质量检查清单**：
- [ ] 模板 props 已深拷贝，未修改原始模板
- [ ] 源品牌 token 已全部替换（无 "Siemens"、"Audeze" 等残留）
- [ ] 中文 prompt 对应的模板已本地化
- [ ] 可编辑字段已标记（editableFields 非空）

**禁止事项**：
- 禁止跨 kind 模糊匹配（hero 模板不可用于 products section）
- 禁止跳过个性化直接返回原始模板 props
- 禁止修改模板的结构（仅替换内容，不改布局）

#### 4.6.4 Section Generation Skill（section 生成，对应 slide-making-skill）

**核心能力**：你是网站 section 生成专家，负责将 section 计划转化为可渲染的 Puck block。你掌握模板优先和 LLM 生成两条路径，并在所有路径失败时提供确定性兜底。

**设计原则**：

1. **模板优先**：能用模板解决的 section 绝不调用 LLM，降低成本和延迟
2. **强制模板 section**：navigation/footer/contact/cta 四类 section 强制走模板路径，不经 LLM
3. **LLM 生成有约束**：LLM 输出必须符合 composition preset 的结构约束
4. **永不空白**：任何 section 最终都有确定性 fallback block 兜底

**三策略决策逻辑**：

```
shouldTemplateFirstForSection(section):
  |
  |-- section.kind in [navigation, footer, contact, cta]?
  |     YES --> 强制 template_first（不经 LLM）
  |
  |-- page.strategy == 'template_first'?
  |     YES --> template_first
  |
  |-- page.strategy == 'llm_first'?
  |     YES --> llm_first（但失败仍 fallback 到 template）
  |
  |-- page.strategy == 'hybrid'?
        YES --> 先尝试 template，无命中则 LLM
```

**template_first 路径**：

```typescript
async function createTemplateSectionResult(section, context) {
  // 1. 调用 Template Matching Skill
  const asset = await resolveSectionTemplateAsset(section, context);

  if (asset) {
    // 2. 应用 composition preset 约束
    applyCompositionPresetConstraints(asset.block, section.preset);

    // 3. 注入 RAG context 到文案字段
    if (context.ragContext) {
      enrichBlockWithRagContent(asset.block, context.ragContext);
    }

    return {
      block: asset.block,
      source: 'template',
      layer: asset.layer,
      editableFields: asset.editableFields
    };
  }

  // 无模板命中，降级到 LLM 或 fallback
  return null;
}
```

**llm 路径**：

```typescript
async function generateSectionViaLlm(section, context) {
  // 1. 构建 section prompt
  const prompt = buildSectionPrompt({
    sectionKind: section.kind,
    compositionPreset: section.preset,    // 如 "H01: split showcase"
    pageContext: context.pageType,
    ragContext: context.ragContext,        // scoped RAG 结果
    theme: context.theme,                 // 只读主题引用
    constraints: section.hardnessRules    // 页面级约束
  });

  // 2. LLM tool call（builder_section tool）
  const result = await callLlm(prompt, {
    tool: 'builder_section',
    maxTokens: 4096,
    timeout: 30000
  });

  // 3. 解析修复
  const block = parseSectionBlock(result);
  if (!block) throw new Error('LLM output parse failed');

  // 4. Schema 校验
  validateBlockSchema(block, section.kind);

  return { block, source: 'llm', editableFields: inferEditableFields(block) };
}
```

**确定性 Fallback Block**（~700 行硬编码）：

| Section Kind | Fallback Block Type | 结构 |
|-------------|--------------------|----|
| navigation | Navbar | logo + 5 nav links + CTA button |
| hero | HeroSplit | 标题 + 副标题 + CTA + 右侧图片占位 |
| story | ContentSplit | 左文右图，3 段叙事 |
| approach | CardsGrid | 3 列卡片，icon + 标题 + 描述 |
| products | CardsGrid | 3 列产品卡片，图片 + 名称 + 描述 + 链接 |
| socialproof | TestimonialCards | 3 列客户评价卡片 |
| contact | ContactForm | 表单（姓名/邮箱/电话/消息）+ 联系信息 |
| cta | CTABanner | 标题 + 描述 + 主/次 CTA 按钮 |
| footer | Footer | 4 列链接 + 版权信息 + 社交图标 |

**并发控制**：每页内 section 并发 `concurrency=3`，通过 semaphore 控制。

**质量检查清单**：
- [ ] 强制模板 section（nav/footer/contact/cta）未经 LLM
- [ ] LLM 输出已通过 JSON parse + schema validate
- [ ] 所有 section 有 block 输出（无空 section）
- [ ] fallback block 已标记 `source: 'fallback'`（供 Payload Audit 检测）
- [ ] 中文 prompt 的 section 内容为中文

**禁止事项**：
- 禁止 navigation/footer section 走 LLM 路径
- 禁止返回空 block（必须有 fallback）
- 禁止 LLM 生成的 block 包含 `{{placeholder}}` 或 `lorem ipsum`

#### 4.6.5 Navbar/Footer Skill（全局 Chrome，对应 ppt-orchestra-skill 的一致性检查）

**核心能力**：你是网站全局导航与页脚一致性专家。你确保所有页面的导航栏和页脚结构完全一致，链接正确，品牌元素统一。

**设计原则**：

1. **全站一致**：所有页面的 navbar 和 footer 必须结构相同、链接相同
2. **链接可达**：navbar/footer 中的所有内链必须指向站内已存在的页面
3. **外链受控**：非 trusted hosts 的外链一律回退为内链
4. **最后执行**：在所有页面生成完成后统一处理，不在 Page Builder 内处理

**输入**：
```typescript
{
  pages: GeneratedPage[];           // 所有已生成的页面
  sitePlan: SitePlan;               // 站点计划（含 linkGraph）
  theme: ThemeConfig;               // 锁定的主题
}
```

**输出**：
```typescript
{
  unifiedNavbar: {
    logo: { text: string; href: '/' };
    links: Array<{ label: string; href: string }>;
    cta: { label: string; href: string } | null;
  };
  unifiedFooter: {
    columns: Array<{
      title: string;
      links: Array<{ label: string; href: string }>;
    }>;
    copyright: string;
    socialLinks: Array<{ platform: string; href: string }>;
  };
}
```

**处理流程**：

```
1. 构建 SiteLinkGraph
   |-- navigationLinks: 从 sitePlan.pages 提取（path + name）
   |-- footerColumns: 按页面类型分组（产品/方案/公司/法律）
   |-- validInternalHrefs: 所有已生成页面的 canonical path 集合

2. 统一 Navbar
   |-- 遍历所有页面的 navigation section
   |-- 替换 links 为 SiteLinkGraph.navigationLinks
   |-- 替换 logo href 为 /
   |-- 替换 CTA 为统一的 CTA（通常指向 /contact）
   |-- 确保所有页面的 navbar props 完全相同

3. 统一 Footer
   |-- 遍历所有页面的 footer section
   |-- 替换 columns 为 SiteLinkGraph.footerColumns
   |-- 替换 copyright 为当前年份 + 公司名
   |-- 确保所有页面的 footer props 完全相同

4. 链接清洗
   |-- 所有 href canonical 化（route-contract）
   |-- 非 validInternalHrefs 的内链 fallback 到 /
   |-- 非 trusted hosts 的外链 fallback 到内链
   |-- 所有 href 小写 + 去尾斜杠
```

**质量检查清单**：
- [ ] 所有页面的 navbar links 完全相同
- [ ] 所有页面的 footer columns 完全相同
- [ ] navbar/footer 中无死链（所有 href 在 validInternalHrefs 中）
- [ ] 无非 trusted 外链残留
- [ ] logo 指向 /
- [ ] CTA 按钮存在且指向有效页面

**禁止事项**：
- 禁止不同页面有不同的 navbar 结构
- 禁止 footer 包含未在站内生成的页面链接
- 禁止外链出现在 navbar 主导航中

#### 4.6.6 Link Normalization Skill（链接规范化）

**核心能力**：你是站内链接治理专家，负责确保全站所有链接的一致性、可达性和安全性。

**设计原则**：

1. **Canonical 唯一**：每个页面只有一个标准路由，所有别名自动映射
2. **内链优先**：不确定的链接一律回退为内链
3. **安全过滤**：外链仅允许 trusted hosts

**Route Contract 映射表**：

| 输入路由 | Canonical 路由 | 说明 |
|---------|---------------|------|
| `/home`、`/index` | `/` | 首页统一 |
| `/product`、`/product-line` | `/products` | 产品页统一 |
| `/custom-solutions`、`/services` | `/solutions` | 方案页统一 |
| `/applications`、`/case-studies` | `/cases` | 案例页统一 |
| `/company`、`/team` | `/about` | 关于页统一 |
| `/get-in-touch`、`/quote` | `/contact` | 联系页统一 |
| `/privacy-policy`、`/terms` | `/privacy` | 法律页统一 |

**处理规则**：

```
1. Canonical 化
   input href --> lowercase --> trim trailing slash --> route-contract 映射

2. 内链校验
   if href in validInternalHrefs --> 保留
   if href not in validInternalHrefs --> fallback 到 /

3. 外链过滤
   if href is absolute URL:
     if host in trustedHosts --> 保留
     else --> 替换为 / (首页内链)

4. 格式规范
   所有 href 小写
   去除尾部斜杠（/ 除外）
   去除查询参数和 hash（站内链接）
```

**质量检查清单**：
- [ ] 无路由别名残留（/home、/product 等已全部 canonical 化）
- [ ] 无死链（所有内链指向已生成页面）
- [ ] 无非 trusted 外链
- [ ] 所有 href 格式统一（小写、无尾斜杠）

**禁止事项**：
- 禁止保留未 canonical 化的路由别名
- 禁止在 navbar/footer 中出现非 trusted 外链
- 禁止内链指向未生成的页面（除非 fallback 到 /）

---

#### 4.6.7 Page Builder Agent（每页生成器，对应 pptx-plugin 的 cover/content/summary/section-divider/toc agent）

**核心能力**：你是网站单页生成专家。你接收 Site Planner 的 PagePlan，为该页面的每个 section 完成模板匹配或 LLM 生成，并执行页面级契约校验。每种 page type 有专属的生成规则。

**设计原则**：

1. **独立执行**：每个 Page Builder 在独立 subgraph 中运行，与其他页面并行，互不干扰
2. **Scoped Context**：仅使用该页面的 RAG context，不访问其他页面的数据
3. **快速失败**：单个 section 失败立即 fallback，不阻塞该页面其他 section
4. **页面级校验**：所有 section 生成后，执行页面级契约校验

**输入**（通过 Send API 从 Site Planner 传入）：
```typescript
{
  page: PagePlan;                    // 页面计划
  theme: ThemeConfig;                // 只读主题
  globalChrome: GlobalChrome;        // navbar/footer 配置
  structured: StructuredSiteInput;   // 结构化输入
  knowledgeBaseClient?: KnowledgeBaseClient;
}
```

**输出**（通过 Annotation reducer 汇聚到 Global Assembler）：
```typescript
{
  pageResult: {
    path: string;
    pageType: EnterprisePageType;
    blocks: GeneratedBlock[];        // 有序 block 列表
    editableFields: EditableFieldContract[];
    templateMeta: TemplateMeta[];    // 每个 section 的模板来源信息
  };
  errors: string[];
  metrics: {
    templateHitRate: number;         // 模板命中率
    llmCallCount: number;            // LLM 调用次数
    fallbackCount: number;           // fallback 次数
    generationTimeMs: number;        // 生成耗时
  };
}
```

**执行流程**：

```
Page Builder Subgraph:
  START
    |
    v
  [Scoped RAG Node]
    |-- 检查 StructuredSiteInput 已覆盖字段
    |-- 企业知识库检索（如可用）
    |-- Serper 搜索（仅补充缺失字段）
    |-- 输出：ragContext（仅该页面相关信息）
    |
    v
  [Generate Sections Node]
    |-- 遍历 page.sections（并发 concurrency=3）
    |-- 每个 section:
    |     |-- shouldTemplateFirst?
    |     |     YES --> Template Matching Skill --> 个性化 --> 返回 block
    |     |     NO  --> Section Generation Skill (LLM) --> 返回 block
    |     +-- 失败 --> buildDeterministicFallbackBlock()
    |-- 输出：blocks[]
    |
    v
  [Page Contract Node]
    |-- Schema 校验：section 结构符合 page type 规则
    |-- 占位文案检测：无 lorem ipsum / Your Brand / {{...}}
    |-- Page-type 专属校验：
    |     |-- home: hero 包含公司名称
    |     |-- products: 产品数据完整
    |     |-- contact: 有 lead capture + 无渐变文本
    |     |-- privacy: 存在政策文本
    |-- error 级别 --> 整页 fallback
    |-- warning 级别 --> 记录但通过
    |
    v
  END --> 输出 pageResult
```

**Page Type 专属行为**：

| Page Type | 专属行为 |
|-----------|---------|
| **home** | hero 强制 H01 preset（split showcase）；必须有 CTA section；products section 展示前 3 个核心产品 |
| **products** | 当 structured.products 存在时，products section 必须展示所有产品；支持 catalog 分页（catalogPageSize） |
| **solutions** | approach section 必须包含 >= 3 个方案要点；与 cases 页防同构（不同 list 类型） |
| **cases** | socialproof section 优先使用 structured.cases 数据；与 solutions 页防同构 |
| **about** | story section 推荐包含时间线（timeline preset）；team section 可选 |
| **contact** | 强制 template_first；contact section 必须有表单；禁止渐变文本；hardness 注入 `forbidGradientText: true` |
| **privacy** | 纯文本页；强制 template_first + deterministic fallback；最简结构（nav + content + footer） |
| **pricing** | products section 渲染为定价表（3-tier preset）；支持 feature comparison |
| **support** | approach section 渲染为 FAQ accordion；contact section 渲染为工单入口 |

**质量检查清单**（每个 Page Builder 执行）：
- [ ] 所有 section 有 block 输出（无空 section）
- [ ] 必须 section 全部存在（如 home 的 hero/cta）
- [ ] 无占位文案残留
- [ ] 中文 prompt 对应中文内容
- [ ] contact 页有 lead capture 且无渐变文本
- [ ] fallback block 已标记 source（供 Payload Audit 检测）
- [ ] section 数量在 hardnessRules 允许范围内

**禁止事项**：
- 禁止访问其他页面的 RAG context 或生成结果
- 禁止修改 theme
- 禁止 contact 页使用 LLM 生成 contact section
- 禁止返回空 blocks 列表

---

#### 4.6.8 Global Assembler Skill（全站组装，对应 ppt-orchestra-skill 的 text-based QA）

**核心能力**：你是全站质量把关专家。你在所有页面生成完成后，执行全局统一化和三层质量门禁，确保输出的站点可发布。

**设计原则**：

1. **统一化优先**：先统一 navbar/footer/theme，再做质量评估
2. **三层门禁递进**：Contract Gate（硬约束）-> QA Gate（评分）-> Payload Audit（结构安全）
3. **阻断而非修复**：门禁失败时阻断发布，不尝试自动修复（避免引入新问题）

**输入**：
```typescript
{
  pageResults: PageResult[];         // 所有页面的生成结果（从 reducer 汇聚）
  sitePlan: SitePlan;
  theme: ThemeConfig;
  structured: StructuredSiteInput;
}
```

**输出**：
```typescript
{
  pages: FinalPage[];                // 统一化后的最终页面
  components: ComponentCode[];       // 生成的组件代码
  theme: ThemeConfig;
  siteBlueprint: SiteBlueprint;
  qaReport: QAReport;
  audit: PayloadAudit;
  publishStatus: 'ok' | 'blocked';
  errors: string[];
}
```

**执行流程**：

```
Global Assembler:
  |
  |-- 1. 统一 Navbar/Footer（Navbar/Footer Skill）
  |     所有页面注入相同的 navbar/footer props
  |
  |-- 2. Link Normalization（Link Normalization Skill）
  |     所有页面的所有 href canonical 化 + 死链修复
  |
  |-- 3. Theme 锁定校验
  |     确认所有页面引用的 theme 与 SitePlan.theme 一致
  |
  |-- 4. Contract Gate（硬约束，失败即阻断）
  |     |-- 必须有首页 /
  |     |-- 企业站必须有 /privacy
  |     |-- contact 页必须有 lead capture
  |     |-- contact 页无渐变文本
  |     |-- 无路由重复
  |     |-- 语言一致性（中文 prompt -> 中文内容）
  |     |-- 无模板占位文案（lorem ipsum / Your Brand / {{...}}）
  |     +-- 任一 error --> publishStatus = 'blocked'
  |
  |-- 5. QA Gate（评分，低于阈值记录 warning）
  |     |-- coverageScore: 缺页 + navbar/footer 覆盖（阈值 0.85）
  |     |-- linkIntegrityScore: 内链可达性（阈值 0.95）
  |     |-- themeConsistencyScore: 跨页主题一致性（阈值 0.9）
  |     |-- semanticFidelityScore: 品牌命中 + 源品牌泄漏 + 占位文案（阈值 0.9）
  |     |-- localeConsistencyScore: 多语言一致性
  |     |-- middleSectionScore: 中间 section 质量
  |     +-- overallScore: 加权总分（阈值 0.9）
  |
  |-- 6. Payload Audit（结构安全，失败即阻断）
  |     |-- 无 unknown block type
  |     |-- 无 fallback block 残留（CreationFallbackSection）
  |     |-- 无重复 page path
  |     |-- 结构相似度不超标（跨页 shape/role 相似度）
  |     |-- 关键页面对无同构
  |     |-- contact 页无渐变违禁
  |     +-- audit.ok == false --> publishStatus = 'blocked'
  |
  +-- 7. 输出最终结果
```

**质量检查清单**：
- [ ] 所有页面 navbar 结构完全一致
- [ ] 所有页面 footer 结构完全一致
- [ ] Contract Gate 0 error
- [ ] QA overallScore >= 0.9
- [ ] Payload Audit ok == true
- [ ] 无 fallback block 残留在最终输出中
- [ ] 关键页面对（products/core-product、solutions/cases）结构不同构

**禁止事项**：
- 禁止在门禁失败时自动修复并放行（必须阻断或降级）
- 禁止跳过任何一层门禁
- 禁止在 Global Assembler 中修改页面内容（仅统一 chrome + 校验）

---

## 五、与当前 Skill 分支的差异总结

| 维度 | 当前 Skill 分支 | 重构方案 |
|------|----------------|---------|
| 执行模型 | 单体 builder 串行 | Send API Map-Reduce 并行 |
| RAG 粒度 | 全局 Fact Pack 一次注入 | Per-page Scoped RAG |
| Page Builder | 单一 builderNode 处理所有页面 | 每页独立 subgraph |
| 契约校验 | 3 次全站级 normalize + 1 次 validate | Per-page 同步校验 + Per-site 异步校验 |
| 故障隔离 | 单页失败可能阻塞全站 | 单页失败 fallback，不影响其他页面 |
| 并发度 | section 级 concurrency=3 | page 级全并行 + section 级 concurrency=3 |
| HITL | 无 | Planner 后可选断点 |
| Skill 分解 | skill-orchestrator 是纯函数编排器 | 每页一个 Page Builder subgraph + 横切 Theme/Nav/Footer skill |

---

## 六、实施路径

### Phase 1：合并契约体系（1-2 天，低风险）

将 Skill 分支的以下文件合并到 main：
- `contracts.ts`
- `route-contract.ts`
- `page-rule-matrix.ts`
- `skill-orchestrator.ts`
- `structured-input.ts`
- `serper.ts`

在现有 architect/builder 流程中集成，不改变图结构。

**验收**：现有测试通过 + 新增契约校验生效。

### Phase 2：Scoped RAG 改造（1 天，低风险）

1. 在 Site Planner 阶段为每页生成 ragQueries
2. 将 `buildSerperFactPack` 调用从 `generateP2WProject` 入口移到 section 生成前
3. 按页面粒度调用 Serper，结果仅注入当前页面的 prompt

**验收**：token 使用量下降 + 生成质量不退化。

### Phase 3：Page Builder 并行化（3-5 天，中风险）

1. 定义 `PageBuilderState` 和 `pageBuilderGraph` subgraph
2. 将 builderNode 中的页面遍历逻辑提取到 Page Builder subgraph
3. 在 Site Planner 输出端使用 `Send` API fan-out
4. 定义 `Annotation` reducer 汇聚所有页面结果
5. 实现 Global Assembler 节点（navbar/footer 统一 + site-level QA）

**验收**：多页面并行生成 + 单页失败不阻塞 + QA 评分不退化。

### Phase 4：HITL + 流式更新（1-2 天，低风险）

1. 在 Site Planner 后添加 `interrupt()` 断点
2. 实现 SSE/WebSocket 流式推送生成进度
3. 前端展示站点结构预览，用户确认后继续

**验收**：用户可预览并修改站点结构后再生成。

### Phase 5：Page Type 专属 Skill（2-3 天，渐进式）

为高价值页面类型创建专属 skill：
1. `home-page-skill`：hero 变体选择、CTA 策略、above-the-fold 优化
2. `product-page-skill`：catalog 布局、规格表、对比表
3. `contact-page-skill`：表单设计、lead capture 优化

每个 skill 作为 Page Builder subgraph 内的可选增强，通过 `page.pageType` 分发。

**验收**：专属 skill 页面的 QA 评分高于通用生成。

---

## 七、风险与缓解

| 风险 | 严重度 | 缓解策略 |
|------|--------|---------|
| Send API 并行导致 LLM 并发限流 | 高 | 设置 `maxConcurrency` 上限（建议 3-5），配合 provider rate limit |
| Scoped RAG 每页独立调用增加 Serper 成本 | 中 | 设置每页 query budget=2，总预算不超过当前全局调用的 2x |
| Page Builder subgraph 增加代码复杂度 | 中 | 保持 subgraph 接口简洁，复用现有 section 生成逻辑 |
| HITL 断点增加用户等待感知 | 低 | 断点可选（默认关闭），仅在 persist 模式下启用 |
| 过度约束导致无限重试 | 高 | 每页最多重试 2 次，失败即 fallback 模板，不阻塞全站 |
| 并行生成的页面间风格不一致 | 中 | Theme 由 Site Planner 统一输出，所有 Page Builder 只读引用 |

---

## 八、结论

本方案的核心思路是**"PPT 每页一个 Skill" 模式迁移到网站生成**：

1. **Site Planner**（= PPT Orchestra Skill）：全局编排，输出 per-page 生成计划
2. **Page Builder Subgraph**（= PPT Slide Agent）：每页独立并行生成，Scoped RAG + Section 生成 + Page Contract
3. **Global Assembler**（= PPT Deck QA）：全站统一化 + 三层质量门禁
4. **横切 Skills**（= PPT Design Skills）：Theme/Navbar/Footer/Link 作为纯函数工具

相比当前 Skill 分支，本方案在保留其全部契约化优势的基础上，解决了三个核心问题：
- **串行瓶颈** -> Send API 并行
- **全局 RAG 膨胀** -> Scoped RAG
- **故障扩散** -> Per-page 隔离 + fallback

实施建议：Phase 1-2 先行（合并契约 + Scoped RAG，3 天内可完成），Phase 3 作为主要重构（Page Builder 并行化），Phase 4-5 渐进增强。
