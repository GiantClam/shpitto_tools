# 网站生成方案对比评估与最佳方案

## 1. 评估背景

本文档对比 `main` 分支（单体 Builder 方案）与 `codex/shpitto-skill-agent-plan` 分支（Skill 契约化方案）的架构差异，结合代码实现验证、社区最佳实践与专家评审，给出最终推荐方案。

评估维度：
- 架构可扩展性
- 生成质量可控性
- 容错与故障隔离
- 上下文效率（LLM token 利用率）
- 行业最佳实践对齐度

---

## 2. 两版方案核心差异

### 2.1 Main 分支（单体 Builder）

| 维度 | 现状 |
|------|------|
| 图结构 | `START -> architect -> builder -> END`，单体两节点 |
| 输入 | 原始 prompt，无结构化 schema |
| 检索增强 | 无（研究能力在独立 skill 中，未集成到生成管线） |
| 契约验证 | 无站点级契约；模板契约嵌入 `section-template-registry.ts` |
| 路由规范化 | 无显式 route contract |
| 页面规则 | 无 page-rule-matrix / hardness 机制 |
| 质量门禁 | 两层：QA Gate + Payload Audit |
| Skill 加载 | 作为上下文字符串注入 LLM prompt，非解耦执行 |
| 断点续跑 | 有（planning-files） |

### 2.2 Skill 分支（契约化 + Skill 编排）

| 维度 | 新增能力 |
|------|----------|
| 图结构 | 同 `architect -> builder`，但 builder 内部增加契约校验节点 |
| 输入 | `StructuredSiteInput`：company / nav / products / cases / FAQs / contactFields / targetLanguage / catalogPageSize |
| 检索增强 | Serper Fact Pack：按 required fields 发 query，注入 External Fact Pack |
| 契约验证 | `contracts.ts`：`normalizePagesBySiteContract` + `validateGeneratedSiteContract` |
| 路由规范化 | `route-contract.ts`：canonical 映射（/home→/, /product→/products 等） |
| 页面规则 | `page-rule-matrix.ts` + `skill-orchestrator.ts`：hardness 注入、section 去重预算、关键页面对防同构 |
| 质量门禁 | 三层：Contract Gate → QA Gate（增加 middleSectionScore）→ Payload Audit |
| Sandbox | 增加 skinnable slots 编辑导出 |

### 2.3 代码验证结果

通过对 main 分支代码的全量检索，确认以下功能**仅存在于 Skill 分支**：

| 功能 | main 分支 | skill 分支 |
|------|-----------|------------|
| StructuredSiteInput / parseStructuredSiteInput | ❌ 不存在 | ✅ |
| Serper 集成 (buildSerperFactPack, SERPER_*) | ❌ 不存在 | ✅ |
| 站点契约 (validateGeneratedSiteContract) | ❌ 不存在 | ✅ |
| 路由契约 (route-contract.ts) | ❌ 不存在 | ✅ |
| Skill 编排器 (orchestrateTemplateAndSectionCandidates) | ❌ 不存在 | ✅ |
| 页面规则矩阵 (page-rule-matrix / hardness) | ❌ 不存在 | ✅ |

main 分支已有但 skill 分支复用的能力：sandbox-client、skinnable 模板、模板资产契约（template-factory/contracts/）、QA Gate、Payload Audit。

---

## 3. 行业最佳实践对标

### 3.1 LangGraph 多 Agent 编排

- **Supervisor 模式**（LangChain 官方推荐）：中央 supervisor 按能力路由到专业 sub-agent，每个 sub-agent 维护独立 scratchpad。
- **Subgraph 组合**：子图可拥有独立 state schema，通过 input/output 变换桥接父图。
- **Map-Reduce 并行**：对独立页面/section 并行生成，最后汇总。

**对标结论**：Skill 分支的 skill-orchestrator 方向正确，但当前仍在单体 builder 内串行执行，未充分利用 LangGraph 的 subgraph 并行能力。

### 3.2 契约驱动生成

- **Schema-First Pipeline**（2026 行业共识）：所有 LLM 输入/输出必须有显式 schema 约束，而非依赖 prompt 隐式约定。
- **Zod/Pydantic 契约测试**：对 LLM 输出做运行时 schema 校验，失败即重试或降级。

**对标结论**：Skill 分支的 StructuredSiteInput + Site Contract + Route Contract 完全对齐此最佳实践。Main 分支的原始 prompt 输入是反模式。

### 3.3 检索增强生成（RAG）

- **生产级 RAG**：不应将全部检索结果注入单一 prompt（"lost in the middle" 退化），应按 section/page 粒度做 just-in-time 注入。
- **CodeRAG-Bench** 研究表明：结构化内容生成场景下，RAG 可显著提升信息密度和事实准确性。

**对标结论**：Skill 分支引入 Serper Fact Pack 方向正确，但当前实现为全局注入，存在上下文膨胀风险。

### 3.4 质量门禁

- **多层门禁**是行业标准：schema 校验（同步）→ 语义质量评估（异步）→ 发布前审计。
- 研究数据：79% 的多 Agent 系统失败源于规格/协调问题，而非技术 bug——显式契约可直接缓解。

**对标结论**：Skill 分支的三层门禁（Contract → QA → Audit）完全对齐最佳实践，优于 main 的两层。

---

## 4. 专家评审结论

### 4.1 Skill 分支是否更优？

**是，在架构方向上明确更优。** 具体优势：

1. **确定性提升**：StructuredSiteInput + Site/Route Contract 将隐式约定显式化，降低 LLM 熵导致的不可预测输出。
2. **信息密度提升**：Serper Fact Pack 解决了"用户输入信息不足导致页面空洞"的核心痛点。
3. **结构多样性保障**：page-rule-matrix + hardness + 关键页面对防同构，直接治理页面同构率问题。
4. **故障可诊断性**：三层门禁 + 契约错误分级，使问题定位从"QA 分低"细化到"哪条契约被违反"。

### 4.2 Skill 分支的风险

| 风险 | 说明 | 严重度 |
|------|------|--------|
| 过度约束 | 严格页面规则 + 防同构预算 + 三层门禁可能产生不可满足约束，导致无限重试 | 高 |
| 全局 Fact Pack 膨胀 | 所有 Serper 结果注入主 prompt，token 成本飙升且指令遵循退化 | 高 |
| 延迟恶化 | 串行 RAG + 串行契约校验 + 串行 QA 叠加，轻松超出 HTTP 超时 | 中 |
| 单体 builder 瓶颈 | 虽有 skill-orchestrator，但仍在单一 builder 节点内串行执行 | 中 |

### 4.3 两版方案的反模式

| 反模式 | 所在方案 |
|--------|----------|
| 原始 prompt 无 schema 约束 | Main |
| Skill 作为字符串注入而非独立执行单元 | Main |
| 全局 Fact Pack 一次性注入 | Skill |
| 单体 builder 串行处理所有页面 | 两者均有 |

---

## 5. 最佳方案推荐

### 5.1 总体策略

**采纳 Skill 分支的契约化体系 + 重构执行模型为分层并行架构。**

即：Skill 分支的数据结构和验证体系是正确的，但执行模型需要从"单体串行"演进为"分层并行"。

### 5.2 推荐架构

```
POST /api/creation
    │
    ▼
[StructuredSiteInput 解析]  ← 采纳 Skill 分支
    │
    ▼
[Planner 节点]  ← 替代当前 architect
    ├── Route Contract 规范化
    ├── Site Contract 校验
    ├── Page Rule Matrix 注入
    └── 输出：SitePlan（页面列表 + section skeleton + 每页 RAG query）
    │
    ▼
[HITL 断点]  ← 新增：用户确认站点结构后再执行昂贵生成
    │
    ▼
[Parallel Page Builders]  ← Map 阶段，替代单体 builder
    ├── Page Builder A（home）── Scoped RAG ── Section 生成 ── 页面级契约校验
    ├── Page Builder B（products）── Scoped RAG ── Section 生成 ── 页面级契约校验
    ├── Page Builder C（about）── ...
    └── ...（并行执行）
    │
    ▼
[Global Assembler]  ← Reduce 阶段
    ├── 统一 Navbar / Footer / Theme
    ├── Link Graph 规范化
    ├── 全站 Contract Gate
    └── 全站 QA Gate + Payload Audit
    │
    ▼
[Persist + Sandbox Preview]
```

### 5.3 关键设计决策

| 决策 | 推荐 | 理由 |
|------|------|------|
| 输入 schema | 采纳 StructuredSiteInput | 消除原始 prompt 的不确定性 |
| 站点/路由契约 | 采纳 contracts.ts + route-contract.ts | 显式化硬约束，对齐 schema-first 最佳实践 |
| 检索增强 | 采纳 Serper，但改为 Scoped RAG | 按页面/section 粒度 just-in-time 注入，避免全局膨胀 |
| 页面规则 | 采纳 page-rule-matrix + hardness | 直接治理页面同构率 |
| 执行模型 | 重构为 Map-Reduce 并行 | 页面间无依赖，可并行生成，故障隔离 |
| 质量门禁 | 采纳三层，但分两阶段执行 | 同步：schema/contract 校验（生成中）；异步：语义 QA（组装后） |
| HITL | 新增 Planner 后断点 | 用户确认结构后再执行昂贵生成，避免浪费 |
| 重试策略 | 硬上限 + fallback 模板 | 防止过度约束导致无限重试 |

### 5.4 实施路径

| 阶段 | 内容 | 工作量 |
|------|------|--------|
| Phase 1 | 合并 Skill 分支的数据结构：StructuredSiteInput、Site/Route Contract、Page Rule Matrix | 1-2 天 |
| Phase 2 | 将 Serper Fact Pack 改为 Scoped RAG（按页面粒度检索注入） | 1 天 |
| Phase 3 | 重构 LangGraph 为 Planner → Parallel Page Builders → Global Assembler | 3-5 天 |
| Phase 4 | 添加 HITL 断点 + 流式状态更新 | 1-2 天 |

---

## 6. 结论

Skill 分支在**数据模型、契约体系、检索增强、页面规则**四个维度上明确优于 Main 分支，与 2026 年行业最佳实践高度对齐。但其**执行模型仍为单体串行**，存在延迟、上下文膨胀和故障扩散风险。

**最佳方案 = Skill 分支的契约化体系 + 分层并行执行模型。**

优先级：先合并契约体系（Phase 1-2，低风险高收益），再重构执行模型（Phase 3-4，高收益但需更多验证）。
